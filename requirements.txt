# --- Jupyter (local only; Colab provides its own) ---
jupyterlab==4.*
ipykernel==6.*
ipywidgets==8.*   # enables tqdm's interactive progress bars in JupyterLab

# --- Core IR Stage 1 stack ---
bm25s==0.2.*
ir_datasets==0.5.*
pytrec_eval-terrier==0.5.*

# --- Quality-of-life ---
# numpy comes in transitively via bm25s / ir_datasets / scipy.
# Don't pin: Colab pre-installs many packages that require numpy>=2,
# and a hard pin to 1.26 breaks jax/opencv/rasterio on Colab.
tqdm==4.*

# --- Git hygiene ---
# Strips notebook outputs on commit so .ipynb diffs are signal-only.
# Activate locally with: nbstripout --install
nbstripout==0.7.*
